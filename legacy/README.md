# Legacy Assets

This folder preserves original Colab-era scripts for provenance and comparison.

- `legacy/colab/` contains the historical preprocessing scripts as originally authored.
- They are reference-only and are **not** part of the supported manifest-driven pipeline.
- The supported pipeline is implemented under `src/breastcancer_rep/` and orchestrated via `scripts/`.

